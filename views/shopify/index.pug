extends ../layout

block content
	link(rel='stylesheet', href='https://sdks.shopifycdn.com/polaris/1.14.1/polaris.min.css')
	script.
		window.appData = {
			baseURL: "#{BASE_URL}",
			apiKey: "#{apiKey}",
			shopOrigin: "https://#{shop}",
			shopData: !{shopData}
		}
	#root
	script(src="/assets/main.js" async)
